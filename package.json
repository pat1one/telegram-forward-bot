{
  "name": "telegram-forward-bot",
  "version": "1.0.0",
  "description": "A Telegram bot that receives messages and forwards them to a specific user.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "node-telegram-bot-api": "^0.61.0"
  }
}

